#Todo List
A React app that displays a list of todo items. 

##Installation
- Clone this repo to your machine locally. 
- Install project dependencies by running the command: `npm install`

##Running the development server
- Start the development server by running the command: `npm run dev`
- Open a web browser and navigate to the "Local:" url
